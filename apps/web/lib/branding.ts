/**
 * Default restaurant logo, used when a restaurant has not uploaded its own
 * (logoPath is null). It's a static asset in apps/web/public — create it at
 * public/logo-default.png (square, 1024×1024, PNG with transparent background).
 */
export const DEFAULT_LOGO_PATH = "/logo-default.png";
